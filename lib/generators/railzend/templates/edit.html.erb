.page-header
	%h2 Edit <%= resource_name.capitalize %> #{@<%=resource_name%>.id}
.btn-group
	=link_to 'Show' , <%= (class_path+[resource_name]).join('_')%>_path( @<%=resource_name%> ) , :class => 'btn' 
	=link_to 'New' , new_<%= (class_path+[resource_name]).join('_')%>_path , :class => 'btn'
	=link_to 'Back to Index' , <%= (class_path+[resource_name.pluralize]).join('_')%>_path , :class => 'btn'
%hr	
=render 'form'